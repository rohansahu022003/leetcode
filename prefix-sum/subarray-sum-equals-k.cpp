class Solution {
public:
    int subarraySum(vector<int>& nums, int k) {
        sort(nums.begin(), nums.end());
        int i=0;
        if(numd[0]==k)count++;
        int j=1;
        
        while(j<nums.size()){
           
        }
        
        return count;
    }
};